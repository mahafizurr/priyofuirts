import React from "react";
import CartComponent from "../components/CartComponent"; // Ensure the correct path
import RecommendedProducts from "@/components/RecommendedProducts";

const CheckoutPage = () => {
  // Render the Checkout page

  // Render the Cart component and RecommendedProducts component

  return (
    <div>
      <CartComponent />
      <RecommendedProducts />
    </div>
  );
};

export default CheckoutPage;
